diff --git a/nativelib/src/main/scala/scala/scalanative/runtime/monitor/ObjectMonitor.scala b/nativelib/src/main/scala/scala/scalanative/runtime/monitor/ObjectMonitor.scala
index e42dd6e..5c4a637 100644
--- a/nativelib/src/main/scala/scala/scalanative/runtime/monitor/ObjectMonitor.scala
+++ b/nativelib/src/main/scala/scala/scalanative/runtime/monitor/ObjectMonitor.scala
@@ -137,22 +137,25 @@ private[monitor] class ObjectMonitor() {
       @alwaysinline def MaxPollInterval = 1000000L // ns = 1ms
       var forcedTimedRecheck = node.consumeTimedParkHint()
 
-      NativeThread.currentNativeThread.state =
-        NativeThread.State.WaitingOnMonitorEnter
+      // Cached once: this thread's own native-thread pointer can't change for
+      // the life of the thread, but each `NativeThread.currentNativeThread`
+      // read is a Darwin TLV lookup (tlv_get_addr) -- re-fetching it on every
+      // spin/park iteration of this contended loop was measured (profiling
+      // a self-hosted `package` run) as a double-digit-percent chunk of total
+      // runtime under lock contention.
+      val nativeThread = NativeThread.currentNativeThread
+      nativeThread.state = NativeThread.State.WaitingOnMonitorEnter
       while (!tryLockThenSpin()) {
         if (activeWaiterThread eq null)
           casActiveWaiterThread(null, currentThread)
-        NativeThread.currentNativeThread.state =
-          NativeThread.State.WaitingOnMonitorEnter
+        nativeThread.state = NativeThread.State.WaitingOnMonitorEnter
         if (forcedTimedRecheck || hasUnmountedVThreads()) {
-          NativeThread.currentNativeThread.parkNanos(
-            recheckIntervalMs * 1000000L
-          )
+          nativeThread.parkNanos(recheckIntervalMs * 1000000L)
           recheckIntervalMs = (recheckIntervalMs * 8) min MaxRecheckIntervalMs
           forcedTimedRecheck = false
         } else {
           recheckIntervalMs = 1L
-          NativeThread.currentNativeThread.parkNanos(pollInterval)
+          nativeThread.parkNanos(pollInterval)
           pollInterval = (pollInterval * 4) min MaxPollInterval
         }
         clearSuccessorAndFenceBeforeRetry(currentThread)
diff --git a/nativelib/src/main/scala/scala/scalanative/runtime/monitor/BasicMonitor.scala b/nativelib/src/main/scala/scala/scalanative/runtime/monitor/BasicMonitor.scala
index b15fa9b..f991a3b 100644
--- a/nativelib/src/main/scala/scala/scalanative/runtime/monitor/BasicMonitor.scala
+++ b/nativelib/src/main/scala/scala/scalanative/runtime/monitor/BasicMonitor.scala
@@ -65,7 +65,8 @@ private[runtime] final class BasicMonitor(val lockWordRef: RawPtr)
 
   private def enterMonitor(thread: Thread, threadId: ThreadId) = {
     import NativeThread._
-    currentNativeThread.state = State.WaitingOnMonitorEnter
+    val nativeThread = currentNativeThread
+    nativeThread.state = State.WaitingOnMonitorEnter
     val current = lockWord
     if (current.isInflated) current.getObjectMonitor.enter(thread)
     else {
@@ -76,7 +77,7 @@ private[runtime] final class BasicMonitor(val lockWordRef: RawPtr)
         } else inflate(thread)
       } else lockAndInflate(thread, threadId)
     }
-    currentNativeThread.state = State.Running
+    nativeThread.state = State.Running
   }
 
   @inline def exit(obj: Object): Unit = {
@@ -169,6 +170,14 @@ private[runtime] final class BasicMonitor(val lockWordRef: RawPtr)
       thread: Thread,
       threadId: ThreadId
   ): Unit = {
+    // Cached once per call, not re-read on every backoff iteration: this
+    // thread's own native-thread pointer can't change mid-loop, but each
+    // `NativeThread.currentNativeThread` read is a Darwin TLV lookup
+    // (tlv_get_addr) -- under thin-lock contention (the common case, since
+    // every synchronized/identity-hash access goes through here before a
+    // monitor is ever inflated) re-fetching it every backoff was measured as
+    // a double-digit-percent chunk of total runtime in profiling.
+    lazy val nativeThread = NativeThread.currentNativeThread
     @tailrec @alwaysinline def waitForOwnership(
         yields: Int,
         backoffNanos: Int
@@ -183,7 +192,7 @@ private[runtime] final class BasicMonitor(val lockWordRef: RawPtr)
               Thread.`yield`()
               waitForOwnership(yields, backoffNanos)
             case _ =>
-              NativeThread.currentNativeThread.sleepNanos(backoffNanos)
+              nativeThread.sleepNanos(backoffNanos)
               waitForOwnership(
                 yields,
                 backoffNanos = (backoffNanos * 3 / 2).min(MaxSleepNanos)
