diff --git a/tools/src/main/scala/scala/scalanative/codegen/Metadata.scala b/tools/src/main/scala/scala/scalanative/codegen/Metadata.scala
index d170391..d8310af 100644
--- a/tools/src/main/scala/scala/scalanative/codegen/Metadata.scala
+++ b/tools/src/main/scala/scala/scalanative/codegen/Metadata.scala
@@ -36,9 +36,15 @@ private[scalanative] class Metadata(
   val canAlwaysUseFastITables = rtti.valuesIterator.forall(_.canUseFastITables)
 
   def initTraitIds(): (Seq[Trait], TraitsUniverse.TraitId.Context) = {
+    // Sorted (by Global name -- nir.Global.globalOrdering) so
+    // TraitsUniverse's graph coloring and per-color id assignment (both
+    // driven purely by this Seq's visitation order, see its own comment)
+    // depend only on which traits are reachable, not on
+    // `analysis.infos` (a plain mutable.Map[Global, Info]) 's unstable
+    // hash-bucket iteration order.
     val traits = analysis.infos.valuesIterator.collect {
       case info: Trait => info
-    }.toIndexedSeq
+    }.toIndexedSeq.sortBy(_.name.toString)
     val universe = new TraitsUniverse(traits)
     val ctx = universe.assignIds(ids)
     (traits, ctx)
diff --git a/tools/src/main/scala/scala/scalanative/codegen/VirtualTable.scala b/tools/src/main/scala/scala/scalanative/codegen/VirtualTable.scala
index ee92050..a0ccfff 100644
--- a/tools/src/main/scala/scala/scalanative/codegen/VirtualTable.scala
+++ b/tools/src/main/scala/scala/scalanative/codegen/VirtualTable.scala
@@ -28,6 +28,9 @@ private[codegen] class VirtualTable(cls: linker.Class)(implicit
       impls(sig) = impl
     }
     slots.foreach { sig => addImpl(sig) }
+    // cls.calls is a SortedSet (see Infos.scala) so a new slot's assigned
+    // index depends only on which Sigs are reachable here, not on
+    // insertion order -- already in order, no sortBy needed here.
     cls.calls.foreach { sig =>
       if (cls.targets(sig).size > 1) {
         if (!impls.contains(sig)) {
diff --git a/tools/src/main/scala/scala/scalanative/linker/Infos.scala b/tools/src/main/scala/scala/scalanative/linker/Infos.scala
index b3dee07..4c4f3d8 100644
--- a/tools/src/main/scala/scala/scalanative/linker/Infos.scala
+++ b/tools/src/main/scala/scala/scalanative/linker/Infos.scala
@@ -15,7 +15,28 @@ sealed abstract class ScopeInfo extends Info {
   override def name: nir.Global.Top
   def ty: nir.Type.Ref = nir.Type.Ref(name)
   val members = mutable.UnrolledBuffer.empty[MemberInfo]
-  val calls = mutable.Set.empty[nir.Sig]
+  // SortedSet (ordered by Sig.mangle, the only stable key Sig has -- Sig
+  // itself has no Ordering, so one's given locally here rather than adding
+  // a top-level implicit -- nir/Sig.scala isn't in either patch's source
+  // list) so vtable-slot assignment (VirtualTable.scala) and itable method
+  // layout (ITable.scala) -- both of
+  // which number entries by `methods`'/this set's iteration position --
+  // depend only on which Sigs are reachable, not on insertion order (calls
+  // is populated incrementally as Reach's single-threaded worklist visits
+  // call sites; an unrelated edit elsewhere in the reachable graph can
+  // reorder that walk's visitation order for the exact same final reachable
+  // Sig set, silently reshuffling every embedded vtable/itable index
+  // otherwise). Keeping the set itself sorted (insert is O(log n)) means
+  // `methods` -- read twice per trait in ITable.scala, once per class in
+  // VirtualTable.scala and Lower.scala -- no longer pays an O(n log n)
+  // sortBy on every read; it's already in order.
+  // Declared type stays mutable.Set, not SortedSet, matching `subclasses`
+  // below: keeps callers outside SHARED_SOURCES binary-compatible against
+  // the stock published jar's erased `Set`-returning signature.
+  val calls: mutable.Set[nir.Sig] = {
+    implicit val sigOrdering: Ordering[nir.Sig] = Ordering.by(_.mangle)
+    mutable.SortedSet.empty[nir.Sig]
+  }
   val responds = mutable.Map.empty[nir.Sig, nir.Global.Member]
 
   def methods = calls.toSeq.filter(_.isVirtual)
@@ -112,7 +133,31 @@ final class Class(
 )(implicit val position: nir.SourcePosition)
     extends ScopeInfo {
   val implementors = mutable.SortedSet[Class](this)
-  val subclasses = mutable.Set.empty[Class]
+  // Was mutable.Set (hash-bucketed by identity hashCode, since Class has no
+  // custom equals/hashCode) -- Metadata.initClassIdsAndRanges walks this set
+  // directly, with no sort of its own, to DFS-number every reachable class
+  // (the dense "class id" embedded in each class's own RTTI struct, plus
+  // the matching subtree instanceof range and @__modules table index --
+  // see RuntimeTypeInformation.scala/ModuleArray.scala). Identity hashCodes
+  // aren't reproducible build-to-build, so that numbering -- and therefore
+  // the emitted bytes for every class's metadata and every instanceof/
+  // dispatch site referencing it -- silently reshuffled on almost any
+  // unrelated change elsewhere in the reachable graph, defeating
+  // IncrementalCodeGenContext's cache almost completely (confirmed
+  // empirically: a same-length string-literal edit in an app's own
+  // Main.scala, with an IDENTICAL reachable class/method count before and
+  // after, changed this same class's own `classId` field from 933 to 4825
+  // between two otherwise-identical builds). SortedSet (mirroring
+  // `implementors` above, same Class.classOrdering -- lexicographic by
+  // Global name) makes DFS sibling-visitation order deterministic; this is
+  // safe for the instanceof range-check invariant, which only needs
+  // pre-order-with-contiguous-subtrees, not any particular sibling order.
+  // Declared type stays mutable.Set (not SortedSet) -- callers outside
+  // SHARED_SOURCES (e.g. Reach.scala) are compiled separately against the
+  // stock published jar's erased `Set`-returning signature for this field;
+  // widening it back here keeps that call site binary-compatible while
+  // still getting deterministic (sorted) iteration at runtime.
+  val subclasses: mutable.Set[Class] = mutable.SortedSet.empty[Class]
   val defaultResponds = mutable.Map.empty[nir.Sig, nir.Global.Member]
   var allocations = 0
   def allocated = allocations > 0
diff --git a/tools/src/main/scala/scala/scalanative/linker/Reach.scala b/tools/src/main/scala/scala/scalanative/linker/Reach.scala
index 4a3673d..ae58be9 100644
--- a/tools/src/main/scala/scala/scalanative/linker/Reach.scala
+++ b/tools/src/main/scala/scala/scalanative/linker/Reach.scala
@@ -85,7 +85,12 @@ private[linker] class Reach(
         linkCppRuntime = linkCppRuntime,
         preprocessorDefinitions = preprocessorDefinitions.toSeq,
         defns = defns.toSeq,
-        dynsigs = dynsigs.toSeq,
+        // Sorted (by mangle) for the same reason ScopeInfo.methods is
+        // (Infos.scala) -- DynmethodPerfectHashMap (PerfectHashMap.scala)
+        // assigns each signature's dynmap slot by this Seq's position, and
+        // `dynsigs`' own hash-bucket iteration order is insertion-sequence-
+        // dependent, not just final-content-dependent.
+        dynsigs = dynsigs.toSeq.sortBy(_.mangle),
         dynimpls = dynimpls.toSeq,
         resolvedVals = resolvedNirValues,
         foundServiceProviders = foundServiceProviders
