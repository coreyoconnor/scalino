diff --git a/tools/src/main/scala/scala/scalanative/codegen/Lower.scala b/tools/src/main/scala/scala/scalanative/codegen/Lower.scala
index c4e14c6..f3ecdd2 100644
--- a/tools/src/main/scala/scala/scalanative/codegen/Lower.scala
+++ b/tools/src/main/scala/scala/scalanative/codegen/Lower.scala
@@ -50,11 +50,15 @@ private[scalanative] object Lower {
     private val currentDefnGraph = new util.ScopedVar[Graph]
     private implicit val currentDefn: util.ScopedVar[nir.Defn.Define] = new util.ScopedVar()
     private implicit val intrinsicMethods: util.ScopedVar[mutable.Map[nir.Local, IntrinsicCall]] = new util.ScopedVar()
-    private val blockInfo = mutable.Map.empty[Block, BlockInfo]
+    // Keyed by Block.id.id (a Long), not the Block itself: a Block's default
+    // case-class hashCode/equals structurally walks its whole `insts` list,
+    // and this map is looked up on nearly every pointer-dereferencing
+    // instruction in the program via isNullGuarded/getCurrentBlockInfo.
+    private val blockInfo = mutable.LongMap.empty[BlockInfo]
     private var currentBlock: Block = _
     private def getCurrentBlockInfo: BlockInfo = {
       assert(currentBlock != null)
-      blockInfo.getOrElseUpdate(currentBlock, new BlockInfo())
+      blockInfo.getOrElseUpdate(currentBlock.id.id, new BlockInfo())
     }
     class BlockInfo(
         val nullGuardedVals: mutable.Set[nir.Val] = mutable.Set.empty
@@ -62,20 +66,22 @@ private[scalanative] object Lower {
     private def findNonRecursive(
         current: Block,
         predicate: BlockInfo => Boolean,
-        visited: mutable.Set[Block] = mutable.Set.empty
-    ): Option[BlockInfo] = blockInfo.get(current) match {
+        visited: mutable.LongMap[Unit] = mutable.LongMap.empty
+    ): Option[BlockInfo] = blockInfo.get(current.id.id) match {
       case Some(info) if predicate(info) => Some(info)
       case _                             =>
-        if (visited.add(current))
+        val id = current.id.id
+        if (!visited.contains(id)) {
+          visited(id) = ()
           current.pred.iterator
             .map(findNonRecursive(_, predicate, visited))
             .collectFirst { case Some(found) => found }
-        else None
+        } else None
     }
     def isNullGuarded(currentBlock: Block, v: nir.Val): Boolean = {
       def isHandled(block: BlockInfo): Boolean =
         block.nullGuardedVals.contains(v)
-      blockInfo.get(currentBlock).exists(isHandled) ||
+      blockInfo.get(currentBlock.id.id).exists(isHandled) ||
         currentBlock.pred.nonEmpty && currentBlock.pred.forall {
           findNonRecursive(_, isHandled).isDefined
         }
diff --git a/tools/src/main/scala/scala/scalanative/interflow/Eval.scala b/tools/src/main/scala/scala/scalanative/interflow/Eval.scala
index f2b84c8..6d39ff4 100644
--- a/tools/src/main/scala/scala/scalanative/interflow/Eval.scala
+++ b/tools/src/main/scala/scala/scalanative/interflow/Eval.scala
@@ -1067,16 +1067,16 @@ private[interflow] trait Eval { self: Interflow =>
         defn.insts.head: @unchecked
 
       val canStoreTo = mutable.Set(self)
-      val arrayLength = mutable.Map.empty[nir.Local, Int]
+      val arrayLength = mutable.LongMap.empty[Int]
 
       defn.insts.foreach {
         case nir.Inst.Let(n, nir.Op.Arrayalloc(_, init, _), _) =>
           canStoreTo += n
           init match {
             case nir.Val.Int(size) =>
-              arrayLength(n) = size
+              arrayLength(n.id) = size
             case nir.Val.ArrayValue(_, elems) =>
-              arrayLength(n) = elems.size
+              arrayLength(n.id) = elems.size
             case _ =>
               ()
           }
@@ -1133,7 +1133,7 @@ private[interflow] trait Eval { self: Interflow =>
               _
             )
             if canStoreTo.contains(to)
-              && inBounds(arrayLength.getOrElse(to, -1), idx) =>
+              && inBounds(arrayLength.getOrElse(to.id, -1), idx) =>
           true
         case nir.Inst.Let(
               _,
@@ -1146,7 +1146,7 @@ private[interflow] trait Eval { self: Interflow =>
               _
             )
             if canStoreTo.contains(to)
-              && inBounds(arrayLength.getOrElse(to, -1), idx) =>
+              && inBounds(arrayLength.getOrElse(to.id, -1), idx) =>
           canStoreValue(value)
         case nir.Inst.Let(_, nir.Op.Arraylength(nir.Val.Local(to, _)), _)
             if canStoreTo.contains(to) =>
diff --git a/tools/src/main/scala/scala/scalanative/interflow/MergeProcessor.scala b/tools/src/main/scala/scala/scalanative/interflow/MergeProcessor.scala
index f918b1b..ae79e01 100644
--- a/tools/src/main/scala/scala/scalanative/interflow/MergeProcessor.scala
+++ b/tools/src/main/scala/scala/scalanative/interflow/MergeProcessor.scala
@@ -26,7 +26,7 @@ private[interflow] final class MergeProcessor(
       case (nir.Inst.Label(local, _), offset) =>
         local -> offset
     }.toMap
-  val blocks = mutable.Map.empty[nir.Local, MergeBlock]
+  val blocks = mutable.LongMap.empty[MergeBlock]
   val todo = mutable.SortedSet.empty[nir.Local](using Ordering.by(offsets))
 
   object currentSize extends Function0[Int] { // context-cached function
@@ -51,7 +51,7 @@ private[interflow] final class MergeProcessor(
       val label = insts(offsets(id)).asInstanceOf[nir.Inst.Label]
       this.newMergeBlock(label)
     }
-    blocks.getOrElseUpdate(id, newMergeBlock)
+    blocks.getOrElseUpdate(id.id, newMergeBlock)
   }
 
   private def newMergeBlock(label: nir.Inst.Label): MergeBlock =
@@ -299,14 +299,14 @@ private[interflow] final class MergeProcessor(
     todo.isEmpty
 
   def invalidate(rootBlock: MergeBlock): Unit = {
-    val invalid = mutable.Map.empty[nir.Local, MergeBlock]
+    val invalid = mutable.LongMap.empty[MergeBlock]
 
     def visitBlock(from: MergeBlock, block: MergeBlock): Unit = {
       val fromName = from.label.id
       val name = block.label.id
-      if (!invalid.contains(name)) {
+      if (!invalid.contains(name.id)) {
         if (offsets(name) > offsets(fromName)) {
-          invalid(name) = block
+          invalid(name.id) = block
           if (block.cf != null) {
             visitCf(from, block.cf)
           }
@@ -363,7 +363,7 @@ private[interflow] final class MergeProcessor(
     invalid.values.foreach { block =>
       block.incoming = block.incoming.filter {
         case (name, _) =>
-          !invalid.contains(name)
+          !invalid.contains(name.id)
       }
       block.outgoing.clear()
       block.phis = null
@@ -372,7 +372,7 @@ private[interflow] final class MergeProcessor(
       block.cf = null
     }
 
-    todo.retain(!invalid.contains(_))
+    todo.retain((l: nir.Local) => !invalid.contains(l.id))
   }
 
   def updateDirectSuccessors(block: MergeBlock): Unit = {
@@ -498,7 +498,7 @@ private[interflow] final class MergeProcessor(
       val syntheticLabel =
         nir.Inst.Label(syntheticFresh(), Seq(syntheticParam))
       val resultMergeBlock = newMergeBlock(syntheticLabel)
-      blocks(syntheticLabel.id) = resultMergeBlock
+      blocks(syntheticLabel.id.id) = resultMergeBlock
       orderedBlocks += resultMergeBlock
 
       // Update all returning blocks to jump to result block,
