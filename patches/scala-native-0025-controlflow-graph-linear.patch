diff --git a/nir/src/main/scala/scala/scalanative/nir/ControlFlow.scala b/nir/src/main/scala/scala/scalanative/nir/ControlFlow.scala
index c560028..51487f0 100644
--- a/nir/src/main/scala/scala/scalanative/nir/ControlFlow.scala
+++ b/nir/src/main/scala/scala/scalanative/nir/ControlFlow.scala
@@ -43,29 +43,60 @@ private[scalanative] object ControlFlow {
   final class Graph(
       val entry: Block,
       val all: Seq[Block],
-      val find: mutable.Map[Local, Block]
-  )
+      blocksById: mutable.LongMap[Block]
+  ) {
+
+    // `find` is public API (mutable.Map[Local, Block], used well beyond this
+    // file) but DirectCodeGen -- the hot path -- never touches it, only the
+    // legacy Lower.scala backend does. Building it eagerly meant boxing every
+    // block id/Local/Tuple2 on every Graph construction for nothing on the
+    // hot path. Lazy + built once from the unboxed LongMap on first access.
+    lazy val find: mutable.Map[Local, Block] = {
+      val m = mutable.Map.empty[Local, Block]
+      blocksById.foreach { case (id, b) => m(Local(id)) = b }
+      m
+    }
+  }
 
   object Graph {
     def apply(insts: Seq[Inst]): Graph = {
       assert(insts.nonEmpty)
 
-      val locations = {
-        val entries = mutable.Map.empty[Local, (Int, Inst.Label)]
+      // Single linear pass: for every label, record its start index and the
+      // index of the terminating control-flow instruction that closes its
+      // body. Doing this once avoids re-scanning `insts` with `indexWhere`
+      // for every block, which made Graph construction O(n^2) on large
+      // generated functions.
+      //
+      // Keyed by the raw Long id (mutable.LongMap), not by Local itself: a
+      // generic mutable.Map[Local, _] boxes every Local (an AnyVal wrapping
+      // a Long) on every insert/lookup, which shows up as its own hot
+      // allocation site (ControlFlow$Graph$$anon$1.applyOrElse et al.) once
+      // the O(n^2) rescan above is fixed and this becomes the dominant cost
+      // on large generated functions. LongMap has no such boxing.
+      val locations = mutable.LongMap.empty[(Int, Inst.Label)]
+      val blockEnd = mutable.LongMap.empty[Int]
+      locally {
         var i = 0
+        var pendingLabel: Long = -1L
+        var hasPending = false
         insts.foreach { inst =>
           inst match {
-            case inst: Inst.Label => entries(inst.id) = (i, inst)
-            case _                => ()
+            case inst: Inst.Label =>
+              locations(inst.id.id) = (i, inst)
+              pendingLabel = inst.id.id
+              hasPending = true
+            case _: Inst.Cf if hasPending =>
+              blockEnd(pendingLabel) = i
+              hasPending = false
+            case _ => ()
           }
 
           i += 1
         }
-
-        entries
       }
 
-      val blocks = mutable.Map.empty[Local, Block]
+      val blocks = mutable.LongMap.empty[Block]
       var todo = List.empty[Block]
 
       def edge(from: Block, to: Block, next: Next) = {
@@ -76,16 +107,13 @@ private[scalanative] object ControlFlow {
 
       def block(local: Local)(implicit pos: SourcePosition): Block =
         blocks.getOrElseUpdate(
-          local, {
-            val (k, Inst.Label(n, params)) = locations(local)
+          local.id, {
+            val (k, Inst.Label(n, params)) = locations(local.id)
 
             // copy all instruction up until and including
             // first control-flow instruction after the label
             val firstInst = k + 1
-            val body = insts.slice(
-              firstInst,
-              insts.indexWhere(_.isInstanceOf[Inst.Cf], from = firstInst) + 1
-            )
+            val body = insts.slice(firstInst, blockEnd(local.id) + 1)
 
             val block = Block(n, params, body, isEntry = k == 0)
             todo ::= block
@@ -132,21 +160,19 @@ private[scalanative] object ControlFlow {
 
       val entryInst = insts.head.asInstanceOf[Inst.Label]
       val entry = block(entryInst.id)(entryInst.pos)
-      val visited = mutable.Set.empty[Local]
 
+      // No separate "visited" set needed: `block()` only ever pushes a given
+      // local onto `todo` the FIRST time it's discovered (getOrElseUpdate),
+      // so every element popped here is visited exactly once already.
       while (todo.nonEmpty) {
         val block = todo.head
         todo = todo.tail
-        val id = block.id
-        if (!visited(id)) {
-          visited += id
-          visit(block)
-        }
+        visit(block)
       }
 
       val all = insts.collect {
-        case Inst.Label(id, _) if visited.contains(id) =>
-          blocks(id)
+        case Inst.Label(id, _) if blocks.contains(id.id) =>
+          blocks(id.id)
       }
 
       new Graph(entry, all, blocks)
