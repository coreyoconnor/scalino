diff --git a/javalib/src/main/scala/java/lang/Boolean.scala b/javalib/src/main/scala/java/lang/Boolean.scala
index 902df6e..84e922f 100644
--- a/javalib/src/main/scala/java/lang/Boolean.scala
+++ b/javalib/src/main/scala/java/lang/Boolean.scala
@@ -1,6 +1,7 @@
 package java.lang
 
 import java.lang.constant.Constable
+import scala.scalanative.meta.LinktimeInfo
 
 final class Boolean(_rawValue: scala.Boolean)
     extends Comparable[Boolean]
@@ -71,8 +72,20 @@ private object BooleanConstants {
 object Boolean {
   final val TYPE =
     scala.Predef.classOf[scala.scalanative.runtime.PrimitiveBoolean]
-  final def TRUE: Boolean = BooleanConstants.TRUE
-  final def FALSE: Boolean = BooleanConstants.FALSE
+
+  // On 64-bit, TRUE/FALSE are tagged immediates rather than the two real
+  // heap-allocated singletons below, so `Boolean.TRUE`/`FALSE` stay `eq`-
+  // consistent with implicit `true`/`false` autoboxing (both now produce
+  // the same tagged bit pattern). `LinktimeInfo.is32BitPlatform` is a
+  // @resolvedAtLinktime constant, so on 64-bit this is a real link-time
+  // branch - `BooleanConstants` (and its two allocations) is unreachable
+  // and dropped entirely, not merely untaken at runtime.
+  final def TRUE: Boolean =
+    if (LinktimeInfo.is32BitPlatform) BooleanConstants.TRUE
+    else scala.scalanative.runtime.SmiBox.encodeBoolean(true).asInstanceOf[Boolean]
+  final def FALSE: Boolean =
+    if (LinktimeInfo.is32BitPlatform) BooleanConstants.FALSE
+    else scala.scalanative.runtime.SmiBox.encodeBoolean(false).asInstanceOf[Boolean]
 
   @inline def compare(x: scala.Boolean, y: scala.Boolean): scala.Int =
     if (x == y) 0 else if (x) 1 else -1
diff --git a/javalib/src/main/scala/java/lang/Byte.scala b/javalib/src/main/scala/java/lang/Byte.scala
index c01e12b..2da8f02 100644
--- a/javalib/src/main/scala/java/lang/Byte.scala
+++ b/javalib/src/main/scala/java/lang/Byte.scala
@@ -3,6 +3,7 @@ package java.lang
 import java.lang.constant.Constable
 
 import scalanative.runtime.Intrinsics.{byteToUInt, byteToULong}
+import scala.scalanative.meta.LinktimeInfo
 
 final class Byte(_rawValue: scala.Byte)
     extends Number
@@ -233,14 +234,22 @@ object Byte {
   import ByteCache.cache
 
   @inline def valueOf(byteValue: scala.Byte): Byte = {
-    val idx = byteValue - MIN_VALUE
-    val cached = cache(idx)
-    if (cached != null) {
-      cached
+    // On 64-bit, every value tags directly (not just the cache's 256-entry
+    // range) and stays `eq`-consistent with implicit autoboxing of the same
+    // value - see java.lang.Boolean.TRUE/FALSE for why this is a real
+    // link-time branch, not a runtime one.
+    if (LinktimeInfo.is32BitPlatform) {
+      val idx = byteValue - MIN_VALUE
+      val cached = cache(idx)
+      if (cached != null) {
+        cached
+      } else {
+        val newbyte = new Byte(byteValue)
+        cache(idx) = newbyte
+        newbyte
+      }
     } else {
-      val newbyte = new Byte(byteValue)
-      cache(idx) = newbyte
-      newbyte
+      scala.scalanative.runtime.SmiBox.encodeByte(byteValue).asInstanceOf[Byte]
     }
   }
 
diff --git a/javalib/src/main/scala/java/lang/Character.scala b/javalib/src/main/scala/java/lang/Character.scala
index 4f9ba4a..6632f71 100644
--- a/javalib/src/main/scala/java/lang/Character.scala
+++ b/javalib/src/main/scala/java/lang/Character.scala
@@ -4,6 +4,7 @@ import java.lang.constant.Constable
 import java.util.Arrays
 
 import scala.scalanative.runtime.LLVMIntrinsics
+import scala.scalanative.meta.LinktimeInfo
 
 class Character(_rawValue: scala.Char)
     extends java.io.Serializable
@@ -411,18 +412,26 @@ object Character {
   import CharacterCache.cache
 
   def valueOf(charValue: scala.Char): Character = {
-    if (charValue > 127) {
-      new Character(charValue)
-    } else {
-      val idx = charValue.toInt
-      val cached = cache(idx)
-      if (cached != null) {
-        cached
+    // On 64-bit, every value tags directly (not just the cache's 0..127
+    // range) and stays `eq`-consistent with implicit autoboxing of the same
+    // value - see java.lang.Boolean.TRUE/FALSE for why this is a real
+    // link-time branch, not a runtime one.
+    if (LinktimeInfo.is32BitPlatform) {
+      if (charValue > 127) {
+        new Character(charValue)
       } else {
-        val newchar = new Character(charValue)
-        cache(idx) = newchar
-        newchar
+        val idx = charValue.toInt
+        val cached = cache(idx)
+        if (cached != null) {
+          cached
+        } else {
+          val newchar = new Character(charValue)
+          cache(idx) = newchar
+          newchar
+        }
       }
+    } else {
+      scala.scalanative.runtime.SmiBox.encodeChar(charValue).asInstanceOf[Character]
     }
   }
 
diff --git a/javalib/src/main/scala/java/lang/Integer.scala b/javalib/src/main/scala/java/lang/Integer.scala
index c28d751..3da1047 100644
--- a/javalib/src/main/scala/java/lang/Integer.scala
+++ b/javalib/src/main/scala/java/lang/Integer.scala
@@ -4,6 +4,7 @@ import java.lang.constant.{Constable, ConstantDesc}
 
 import scalanative.runtime.Intrinsics.{divUInt, intToULong, remUInt}
 import scalanative.runtime.LLVMIntrinsics
+import scala.scalanative.meta.LinktimeInfo
 
 final class Integer(_rawValue: scala.Int)
     extends Number
@@ -503,18 +504,26 @@ object Integer {
   import IntegerCache.cache
 
   @inline def valueOf(intValue: scala.Int): Integer = {
-    if (intValue.toByte.toInt != intValue) {
-      new Integer(intValue)
-    } else {
-      val idx = intValue + 128
-      val cached = cache(idx)
-      if (cached != null) {
-        cached
+    // On 64-bit, every value tags directly (not just the cache's -128..127
+    // range) and stays `eq`-consistent with implicit autoboxing of the same
+    // value - see java.lang.Boolean.TRUE/FALSE for why this is a real
+    // link-time branch, not a runtime one.
+    if (LinktimeInfo.is32BitPlatform) {
+      if (intValue.toByte.toInt != intValue) {
+        new Integer(intValue)
       } else {
-        val newint = new Integer(intValue)
-        cache(idx) = newint
-        newint
+        val idx = intValue + 128
+        val cached = cache(idx)
+        if (cached != null) {
+          cached
+        } else {
+          val newint = new Integer(intValue)
+          cache(idx) = newint
+          newint
+        }
       }
+    } else {
+      scala.scalanative.runtime.SmiBox.encodeInt(intValue).asInstanceOf[Integer]
     }
   }
 
diff --git a/javalib/src/main/scala/java/lang/Short.scala b/javalib/src/main/scala/java/lang/Short.scala
index 556f3ab..9721cba 100644
--- a/javalib/src/main/scala/java/lang/Short.scala
+++ b/javalib/src/main/scala/java/lang/Short.scala
@@ -4,6 +4,7 @@ import java.lang.constant.Constable
 
 import scalanative.runtime.Intrinsics.{shortToUInt, shortToULong}
 import scalanative.runtime.LLVMIntrinsics
+import scala.scalanative.meta.LinktimeInfo
 
 final class Short(_rawValue: scala.Short)
     extends Number
@@ -240,18 +241,26 @@ object Short {
   private val cache = new Array[java.lang.Short](256)
 
   @inline def valueOf(shortValue: scala.Short): Short = {
-    if (shortValue.toByte.toShort != shortValue) {
-      new Short(shortValue)
-    } else {
-      val idx = shortValue + 128
-      val cached = cache(idx)
-      if (cached != null) {
-        cached
+    // On 64-bit, every value tags directly (not just the cache's -128..127
+    // range) and stays `eq`-consistent with implicit autoboxing of the same
+    // value - see java.lang.Boolean.TRUE/FALSE for why this is a real
+    // link-time branch, not a runtime one.
+    if (LinktimeInfo.is32BitPlatform) {
+      if (shortValue.toByte.toShort != shortValue) {
+        new Short(shortValue)
       } else {
-        val newshort = new Short(shortValue)
-        cache(idx) = newshort
-        newshort
+        val idx = shortValue + 128
+        val cached = cache(idx)
+        if (cached != null) {
+          cached
+        } else {
+          val newshort = new Short(shortValue)
+          cache(idx) = newshort
+          newshort
+        }
       }
+    } else {
+      scala.scalanative.runtime.SmiBox.encodeShort(shortValue).asInstanceOf[Short]
     }
   }
 
diff --git a/nativelib/src/main/scala/scala/scalanative/runtime/SmiBox.scala b/nativelib/src/main/scala/scala/scalanative/runtime/SmiBox.scala
index d52e82c..867e990 100644
--- a/nativelib/src/main/scala/scala/scalanative/runtime/SmiBox.scala
+++ b/nativelib/src/main/scala/scala/scalanative/runtime/SmiBox.scala
@@ -18,7 +18,18 @@ import scala.scalanative.runtime.Intrinsics._
  */
 object SmiBox {
   private final val PayloadShift = 6
+  private final val DiscriminantShift = 1
   private final val DiscriminantMask = 0x1FL
+  private final val TagMask = 1L
+
+  // Mirrors nir.Type.Smi's discriminant assignment - compiler-side and
+  // runtime-side can't share the constant by reference (see class doc), so
+  // this ordering must be kept in sync with nir.Type.Smi by hand.
+  private final val BooleanDiscriminant = 0
+  private final val ByteDiscriminant = 1
+  private final val ShortDiscriminant = 2
+  private final val CharDiscriminant = 3
+  private final val IntDiscriminant = 4
 
   @inline def isTagged(obj: RawPtr): Boolean =
     (castRawPtrToLong(obj) & 1L) != 0L
@@ -59,6 +70,31 @@ object SmiBox {
    *  compiles to `Op.Classalloc`, an entirely separate NIR path untouched
    *  by the tagged-boxing change, so it always allocates a genuine object.
    */
+  // Encode direction: given a primitive value, construct the tagged AnyRef
+  // bit pattern directly - the mirror image of `codegen.Lower`'s
+  // `genSmiBoxOp` (see that method for the compiler-side version of this
+  // exact formula). Used by the 5 cache-backed `valueOf` methods
+  // (java.lang.{Boolean,Byte,Short,Character,Integer} - Float has no JLS-
+  // mandated cache, Long is out of scope for SMI entirely) on 64-bit, so an
+  // *explicit* `Integer.valueOf(x)`-style call gets the same zero-allocation,
+  // tagged-identity treatment as implicit autoboxing already does.
+  @inline private def encode(discriminant: Int, payload: Long): AnyRef = {
+    val encoded =
+      (payload << PayloadShift) | (discriminant.toLong << DiscriminantShift) | TagMask
+    castRawPtrToObject(castLongToRawPtr(encoded))
+  }
+
+  @inline def encodeBoolean(value: scala.Boolean): AnyRef =
+    encode(BooleanDiscriminant, if (value) 1L else 0L)
+  @inline def encodeByte(value: scala.Byte): AnyRef =
+    encode(ByteDiscriminant, value.toLong) // toLong sign-extends, matches decode
+  @inline def encodeShort(value: scala.Short): AnyRef =
+    encode(ShortDiscriminant, value.toLong)
+  @inline def encodeChar(value: scala.Char): AnyRef =
+    encode(CharDiscriminant, value.toLong & 0xFFFFL) // zero-extend: Char is unsigned
+  @inline def encodeInt(value: scala.Int): AnyRef =
+    encode(IntDiscriminant, value.toLong)
+
   def reify(obj: RawPtr): AnyRef = {
     discriminant(obj) match {
       case 0 => new java.lang.Boolean(decodeBoolean(obj))
