diff --git a/nir/src/main/scala/scala/scalanative/nir/Show.scala b/nir/src/main/scala/scala/scalanative/nir/Show.scala
index 69f98b0..2e55855 100644
--- a/nir/src/main/scala/scala/scalanative/nir/Show.scala
+++ b/nir/src/main/scala/scala/scalanative/nir/Show.scala
@@ -42,7 +42,7 @@ object Show {
 
   type DefnString = (Global, String)
 
-  def dump(defns: Seq[Defn], fileName: String): Unit = {
+  def dump(defns: collection.Seq[Defn], fileName: String): Unit = {
     val pw = new java.io.PrintWriter(fileName)
 
     try {
diff --git a/tools/src/main/scala/scala/scalanative/build/ScalaNative.scala b/tools/src/main/scala/scala/scalanative/build/ScalaNative.scala
index dd25e7c..bee5e15 100644
--- a/tools/src/main/scala/scala/scalanative/build/ScalaNative.scala
+++ b/tools/src/main/scala/scala/scalanative/build/ScalaNative.scala
@@ -329,7 +329,7 @@ private[scalanative] object ScalaNative {
     log(s"\n${errors.size} errors found")
   }
 
-  def dumpDefns(config: Config, phase: String, defns: Seq[nir.Defn]): Unit = {
+  def dumpDefns(config: Config, phase: String, defns: collection.Seq[nir.Defn]): Unit = {
     if (config.dump) {
       config.logger.time(s"Dumping intermediate code ($phase)") {
         val path = config.workDir.resolve(phase + ".hnir")
diff --git a/tools/src/main/scala/scala/scalanative/codegen/Generate.scala b/tools/src/main/scala/scala/scalanative/codegen/Generate.scala
index 3110659..28be44a 100644
--- a/tools/src/main/scala/scala/scalanative/codegen/Generate.scala
+++ b/tools/src/main/scala/scala/scalanative/codegen/Generate.scala
@@ -14,14 +14,14 @@ private[codegen] object Generate {
   private implicit val scopeId: nir.ScopeId = nir.ScopeId.TopLevel
   import Impl._
 
-  def apply(entry: Option[nir.Global.Top], defns: Seq[nir.Defn])(implicit
+  def apply(entry: Option[nir.Global.Top], defns: collection.Seq[nir.Defn])(implicit
       meta: Metadata
   ): Seq[nir.Defn] =
     (new Impl(entry, defns)).generate()
 
   implicit def reachabilityAnalysis(implicit meta: Metadata): ReachabilityAnalysis.Result = meta.analysis
 
-  private class Impl(entry: Option[nir.Global.Top], defns: Seq[nir.Defn])(implicit
+  private class Impl(entry: Option[nir.Global.Top], defns: collection.Seq[nir.Defn])(implicit
       meta: Metadata
   ) {
     val buf = mutable.UnrolledBuffer.empty[nir.Defn]
@@ -112,7 +112,7 @@ private[codegen] object Generate {
     /* Generate class initializers to handle class instantiated using reflection */
     private def genClassInitializersCalls(
         unwind: () => nir.Next
-    )(implicit fresh: nir.Fresh): Seq[nir.Inst] = {
+    )(implicit fresh: nir.Fresh): collection.Seq[nir.Inst] = {
       defns.collect {
         case defn @ nir.Defn.Define(_, name: nir.Global.Member, _, _, _) if name.sig.isClinit =>
           nir.Inst.Let(
diff --git a/tools/src/main/scala/scala/scalanative/codegen/GenerateReflectiveProxies.scala b/tools/src/main/scala/scala/scalanative/codegen/GenerateReflectiveProxies.scala
index 0d9a090..0c1a851 100644
--- a/tools/src/main/scala/scala/scalanative/codegen/GenerateReflectiveProxies.scala
+++ b/tools/src/main/scala/scala/scalanative/codegen/GenerateReflectiveProxies.scala
@@ -144,8 +144,8 @@ private[codegen] object GenerateReflectiveProxies {
   }
 
   def apply(
-      dynimpls: Seq[nir.Global],
-      defns: Seq[nir.Defn]
+      dynimpls: collection.Iterable[nir.Global],
+      defns: collection.Seq[nir.Defn]
   ): Seq[nir.Defn.Define] = {
 
     // filters methods with same name and args but different return type for each given type
diff --git a/tools/src/main/scala/scala/scalanative/codegen/PerfectHashMap.scala b/tools/src/main/scala/scala/scalanative/codegen/PerfectHashMap.scala
index b7be930..a118230 100644
--- a/tools/src/main/scala/scala/scalanative/codegen/PerfectHashMap.scala
+++ b/tools/src/main/scala/scala/scalanative/codegen/PerfectHashMap.scala
@@ -169,8 +169,8 @@ private[codegen] class PerfectHashMap[K, V](
 private[codegen] object DynmethodPerfectHashMap {
 
   def apply(
-      dynmethods: Seq[nir.Global.Member],
-      allSignatures: Seq[nir.Sig]
+      dynmethods: collection.Seq[nir.Global.Member],
+      allSignatures: collection.Seq[nir.Sig]
   ): nir.Val = {
 
     val signaturesWithIndex =
diff --git a/tools/src/main/scala/scala/scalanative/linker/ClassLoader.scala b/tools/src/main/scala/scala/scalanative/linker/ClassLoader.scala
index 1ffdfa9..da9a3fb 100644
--- a/tools/src/main/scala/scala/scalanative/linker/ClassLoader.scala
+++ b/tools/src/main/scala/scala/scalanative/linker/ClassLoader.scala
@@ -26,7 +26,7 @@ object ClassLoader {
     new FromDisk(classpath)
   }
 
-  def fromMemory(defns: Seq[nir.Defn]): ClassLoader =
+  def fromMemory(defns: collection.Seq[nir.Defn]): ClassLoader =
     new FromMemory(defns)
 
   final class FromDisk(classpath: Seq[ClassPath]) extends ClassLoader {
@@ -44,7 +44,7 @@ object ClassLoader {
       }.flatten
   }
 
-  final class FromMemory(defns: Seq[nir.Defn]) extends ClassLoader {
+  final class FromMemory(defns: collection.Seq[nir.Defn]) extends ClassLoader {
 
     private val scopes = {
       val out =
diff --git a/tools/src/main/scala/scala/scalanative/linker/Infos.scala b/tools/src/main/scala/scala/scalanative/linker/Infos.scala
index 4c4f3d8..c02bc92 100644
--- a/tools/src/main/scala/scala/scalanative/linker/Infos.scala
+++ b/tools/src/main/scala/scala/scalanative/linker/Infos.scala
@@ -275,27 +275,31 @@ final class Field(
 }
 
 sealed trait ReachabilityAnalysis {
-  def defns: Seq[nir.Defn]
+  def defns: collection.Seq[nir.Defn]
   def foundServiceProviders: FoundServiceProviders
   def isSuccessful: Boolean = this.isInstanceOf[ReachabilityAnalysis.Result]
 }
 
 object ReachabilityAnalysis {
   final class Failure(
-      val defns: Seq[nir.Defn],
-      val unreachable: Seq[Reach.UnreachableSymbol],
-      val unsupportedFeatures: Seq[Reach.UnsupportedFeature],
+      val defns: collection.Seq[nir.Defn],
+      val unreachable: collection.Seq[Reach.UnreachableSymbol],
+      val unsupportedFeatures: collection.Seq[Reach.UnsupportedFeature],
       val foundServiceProviders: FoundServiceProviders
   ) extends ReachabilityAnalysis
   final class Result(
       val infos: mutable.Map[nir.Global, Info],
-      val entries: Seq[nir.Global],
-      val links: Seq[nir.Attr.Link],
+      val entries: collection.Seq[nir.Global],
+      // Sets, not Seqs: both are mutable.Set at the Reach.scala call site --
+      // exposing them as-is skips a Set->Seq copy on every build. Order
+      // doesn't matter for either (see call sites: .exists/.map/.contains),
+      // unlike dynsigs below, whose Seq position IS the dynmap slot index.
+      val links: collection.Set[nir.Attr.Link],
       val linkCppRuntime: Boolean,
       val preprocessorDefinitions: Seq[nir.Attr.Define],
-      val defns: Seq[nir.Defn],
-      val dynsigs: Seq[nir.Sig],
-      val dynimpls: Seq[nir.Global.Member],
+      val defns: collection.Seq[nir.Defn],
+      val dynsigs: collection.Seq[nir.Sig],
+      val dynimpls: collection.Set[nir.Global.Member],
       val resolvedVals: mutable.Map[String, nir.Val],
       val foundServiceProviders: FoundServiceProviders
   ) extends ReachabilityAnalysis {
diff --git a/tools/src/main/scala/scala/scalanative/linker/Link.scala b/tools/src/main/scala/scala/scalanative/linker/Link.scala
index e10d3a6..5413fe2 100644
--- a/tools/src/main/scala/scala/scalanative/linker/Link.scala
+++ b/tools/src/main/scala/scala/scalanative/linker/Link.scala
@@ -6,7 +6,7 @@ import scalanative.util.Scope
 object Link {
 
   /** Load all clases and methods reachable from the entry points. */
-  def apply(config: build.Config, entries: Seq[nir.Global])(implicit
+  def apply(config: build.Config, entries: collection.Seq[nir.Global])(implicit
       scope: Scope
   ): ReachabilityAnalysis =
     Reach(config, entries, ClassLoader.fromDisk(config))
@@ -14,8 +14,8 @@ object Link {
   /** Run reachability analysis on already loaded methods. */
   def apply(
       config: build.Config,
-      entries: Seq[nir.Global],
-      defns: Seq[nir.Defn]
+      entries: collection.Seq[nir.Global],
+      defns: collection.Seq[nir.Defn]
   ): ReachabilityAnalysis =
     Reach(config, entries, ClassLoader.fromMemory(defns))
 
diff --git a/tools/src/main/scala/scala/scalanative/linker/Reach.scala b/tools/src/main/scala/scala/scalanative/linker/Reach.scala
index ae58be9..7e8f320 100644
--- a/tools/src/main/scala/scala/scalanative/linker/Reach.scala
+++ b/tools/src/main/scala/scala/scalanative/linker/Reach.scala
@@ -8,7 +8,7 @@ import scala.collection.mutable
 
 private[linker] class Reach(
     protected val config: build.Config,
-    entries: Seq[nir.Global],
+    entries: collection.Seq[nir.Global],
     protected val loader: ClassLoader
 ) extends LinktimeValueResolver
     with LinktimeIntrinsicCallsResolver {
@@ -80,26 +80,30 @@ private[linker] class Reach(
     if (unreachable.isEmpty && unsupported.isEmpty)
       new ReachabilityAnalysis.Result(
         infos = infos,
-        entries = (entries ++ exports.toSeq).distinct,
-        links = links.toSeq,
+        entries = (entries ++ exports).distinct,
+        links = links,
         linkCppRuntime = linkCppRuntime,
-        preprocessorDefinitions = preprocessorDefinitions.toSeq,
-        defns = defns.toSeq,
+        preprocessorDefinitions = preprocessorDefinitions.toVector,
+        defns = defns,
         // Sorted (by mangle) for the same reason ScopeInfo.methods is
         // (Infos.scala) -- DynmethodPerfectHashMap (PerfectHashMap.scala)
         // assigns each signature's dynmap slot by this Seq's position, and
         // `dynsigs`' own hash-bucket iteration order is insertion-sequence-
-        // dependent, not just final-content-dependent.
-        dynsigs = dynsigs.toSeq.sortBy(_.mangle),
-        dynimpls = dynimpls.toSeq,
+        // dependent, not just final-content-dependent. Stays a sorted,
+        // concrete Vector (not exposed as the bare Set like links/dynimpls,
+        // and Vector rather than the List a bare .toSeq would produce)
+        // precisely because that index assignment depends on stable order
+        // and repeated positional reads (zipWithIndex/lookup by index).
+        dynsigs = dynsigs.toVector.sortBy(_.mangle),
+        dynimpls = dynimpls,
         resolvedVals = resolvedNirValues,
         foundServiceProviders = foundServiceProviders
       )
     else
       new ReachabilityAnalysis.Failure(
-        defns = defns.toSeq,
-        unreachable = unreachable.values.toSeq,
-        unsupportedFeatures = unsupported.values.toSeq,
+        defns = defns,
+        unreachable = unreachable.values.toVector,
+        unsupportedFeatures = unsupported.values.toVector,
         foundServiceProviders = foundServiceProviders
       )
   }
@@ -1182,7 +1186,7 @@ private[scalanative] object Reach {
 
   def apply(
       config: build.Config,
-      entries: Seq[nir.Global],
+      entries: collection.Seq[nir.Global],
       loader: ClassLoader
   ): ReachabilityAnalysis = {
     val reachability = new Reach(config, entries, loader)
